- name: "Edit a dconf setting, and try again if it fails"
  block:
    - name: "{{ task_descrip }}"
      community.general.dconf:
        state: present
  rescue:
    - name: "SECOND TRY - {{ task_descrip }}"
      community.general.dconf:
        state: present
  vars:
    key: "{{ key }}"
    value: "{{ value | default(omit) }}"
  become: true
  become_user: "{{ user_name }}"

